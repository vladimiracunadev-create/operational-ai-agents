# Soporte

Antes de reportar un problema ejecuta:

```bash
operational-agents doctor
operational-agents validate
python -m unittest discover -s tests -v
```

Incluye sistema operativo, versión de Python, comando, salida sanitizada y agente afectado. No adjuntes secretos, prompts privados ni trazas con datos personales.
