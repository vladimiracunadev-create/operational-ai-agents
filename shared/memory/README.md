# Memoria

Los manifests declaran `project` o `local` para el adaptador Claude. La memoria puede conservar decisiones y patrones, pero nunca se trata como fuente autoritativa ni debe almacenar secretos, datos personales o aprobaciones. Cada misión vuelve a verificar estado y permisos actuales.
