# MCP

Las integraciones MCP son opt-in y deben configurarse en el entorno del usuario. Este repositorio no incluye tokens ni servidores activados. Al agregar uno, limite cada agente a herramientas exactas o a `mcp__<server>__*`, documente datos accesibles y mantenga aprobación para acciones externas.
